## // задача №51 тип 12 №27383
var (s, sum, cnt_4) := ('', 0, 0);

for var n := 1 to 50 do
begin
  s := n*'12'+(10-n)*'1'; // самое сложное - генерить последовательность...

  while ('12' in s) do if ('12' in s) then s := s.Replace('12', '4', 1);
    
  for var i := 1 to Length(s) do sum += StrToInt(s[i]);
  
  if (sum = 25) then
    begin
      cnt_4 := s.CountOf('4').Println;
      Println(n, s, sum);
      break;
    end;
  
  sum := 0;
end; 
