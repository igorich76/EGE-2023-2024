{(107)	(А. Богданов). Откройте файл электронной таблицы 9-107.xls, 
содержащей в каждой строке три натуральных числа. 
Выясните, какое количество троек чисел могут являться величинами углов треугольника, 
выраженных в градусах. В ответе запишите только число.(ege9.doc)}
##
uses xlsx;
var t := ReadXLSXAsInts('9-107.xlsx');
var cnt := 0;
for var i:=0 to t.Length-1 do
  begin
    if (t[i,0]+t[i,1]+t[i,2]=180) then cnt += 1;
  end;
Print(cnt);
// Ответ: 2374
